# 项目状态与验收清单

评估基线：`PLANS.md` 的首版成功标准、自动化测试计划、手工验收计划和完成定义。

## 当前结论

当前已发布版本为 `v0.2.0`。中文/英文/AppID 搜索、连续输入与清空、约 1,000 款真实游戏库、5,000 条本地性能门禁、搜索补丁独立失配降级、空缓存安装及 GitHub Release 正式产物均已通过自动化与 Windows 11 真机验收。此前拟列入 `v0.2.1` 的“首次点击配置后菜单偏移或消失”已通过约 3 秒资源统计刷新和 Extendium 跨插件复现确认为 Millennium 上游问题，不再预定本插件补丁版本。该问题当前没有直接影响，延后单独跟踪；主线优先扩充公共词库、完成公共仓库与分发策略审计，并推进 `v1.0.0-rc.1`。只有需要插件侧兼容调整时才发布 `v0.2.1`。

## 已有证据

- [x] AppID 精确匹配、双语/仅中文格式化规则、已有汉字名称保持不变。
- [x] 内置词库、缓存读取、ETag/Last-Modified 请求、失败回退和设置持久化已有实现。
- [x] 左栏限制性补丁、响应式前端存储、设置页面和手动刷新已有实现。
- [x] CI、标签 Release、ZIP 和 SHA-256 打包工作流已有实现。
- [x] TypeScript 类型检查通过。
- [x] 51 项 Vitest 测试和 4 组 Lua 后端测试通过，当前 692 条内置翻译通过校验。
- [x] 从当前可见库中补充 106 款“好评如潮”游戏；默认名称已含中文的条目不重复收录。
- [x] Starlight 正式构建、`.star` 校验、发布打包和 ZIP 内容校验通过。
- [x] 安装、卸载、隐私、故障排查和贡献文档已有基础说明。

## 稳定版前的阻断项

- [x] `v0.1.1` 构建产物和 ZIP 内条目统一命名为 `steam-game-name-zh.star`，CI 与 Release 均检查归档内容并运行 `starlight verify`。
- [x] Lua 后端已有可重复的语法/静态检查，并纳入 `pnpm run check` 和 CI。
- [x] 后端测试覆盖无缓存、损坏缓存、远程 200/304、DNS/TLS/超时、非法远程数据、原子写入失败、非法设置和并发刷新。
- [x] 补丁测试直接读取生产补丁的查找与替换，覆盖 3 份侧栏 fixture、重复应用、无关 UI 安全性，以及后端重复加载/卸载生命周期。
- [x] `v0.1.7` 移除“仅原名”模式；设置改用 Millennium 原生下拉组件，并让左栏挂载响应式名称组件，相关自动化回归已通过。
- [x] 在 Windows 11、Steam 稳定版简体中文界面和目标 Millennium 版本上完成最终测试。
- [x] 70 个游戏的左栏显示和滚动正常，未观察到卡顿。
- [x] Steam 离线模式下内置/本地词库正常显示。
- [x] 验证正常联网首次更新、ETag/304 检查和远程词库热更新。
- [x] 验证远程端点不可达和 8 秒请求超时场景。
- [x] 验证仅左栏发生变化，英文原名搜索、右侧详情页、下载页、右键菜单和启动游戏不受影响。
- [x] 验证禁用和卸载后名称完整恢复，Steam 功能正常。
- [x] 验证补丁失配后安全降级。
- [x] 将运行时补丁结果接入 `patchCompatible`；左栏补丁首次渲染时上报兼容，未命中时保持不兼容状态并在设置页提示。
- [x] 验证远程词库更新无需重启即可生效。
- [x] 从 GitHub Release 在清空插件缓存的 Millennium 环境安装 ZIP；确认 `v0.1.8-rc.2`、远程首次取得 78 条词库及游戏名称显示正确。
- [x] 从 GitHub Release 下载并校验 `v0.1.8` 稳定版 ZIP；确认 184 条词库、新增译名、设置页和 Steam 基础交互正常。

## 已确认版本路线

### `v0.1.7`：显示模式修复

- 只保留“中文名 | 原名”和“仅中文名”。
- 修复切换后左栏不即时重新渲染。
- 验证设置持久化和历史 `original` 配置迁移。
- 不加入中文搜索。

### `v0.1.8-rc.2` / `v0.1.8`：核心版最终验收

- 完成 GitHub 不可达/超时、远程热更新、禁用/卸载、补丁失配和干净安装测试。
- RC 只接受阻断性修复，不增加新功能。

### `v0.2.0`：中文搜索

- 中文译名与英文原名均可过滤游戏库左栏。
- 搜索与显示模式解耦，并使用独立补丁和 fixture。
- 搜索补丁失败时只降级为英文搜索，不影响中文名称显示。

### Millennium 插件菜单启动期竞态：上游问题

- [x] 确认偏移的是 Millennium 插件操作菜单，不是本插件配置内容或 `Dropdown`。
- [x] 记录启动期插件大小和 CPU 徽章约每 3 秒闪动，菜单偏移/消失集中发生在该阶段，等待后概率明显降低。
- [x] 使用 Extendium 完成跨插件复现，排除本插件词库初始化、状态存储和设置组件为主要原因。
- [x] 将问题标记为上游已知问题并提供等待启动稳定后再配置的临时规避说明。
- [ ] 延后单独向 Millennium 提交包含截图、版本、刷新周期、跨插件证据和复现步骤的问题报告；不阻断当前 RC 主线。
- [ ] 跟踪上游修复，在 Steam Stable/Beta 上对本插件与 Extendium 复验。
- [ ] 仅在上游要求或存在可靠插件侧兼容方案时建立并发布 `v0.2.1`；禁止用固定延时或重复点击掩盖问题。

### `v1.0.0-rc.1` / `v1.0.0`：公共发布与 PluginDatabase 准入

- [x] 解析用户 2026-08-25 Steam 库导出：1,290 项中识别 1,083 款游戏；首批从 625 个英文差集中筛入 99 个高置信译名，且不复制账户、路径或拥有者信息。
- [x] 导入剩余 51 款好评率不低于 90% 的候选；对没有统一中文标题的项目使用保守音译或描述性个人译名，测试版和序章保留明确后缀。
- [x] 导入剩余 242 款好评率为 80–89% 的候选；只处理简体中文环境导出中仍不含汉字的游戏。
- [x] 用户确认 574 条测试构建在 Steam 实机增加成功；继续导入剩余 118 款好评率为 70–79% 的候选。
- [x] 移除 `Grand Theft Auto V 传承版` 和 `全面战争：三国` 两条冗余映射；凡简体中文环境默认名称已含汉字，均保留 Steam 原名而不进入词库。
- [ ] 对剩余 115 个低于 70% 或暂无评价的英文候选继续分批审核；已生成按好评率排序的脱敏清单。
- [ ] 建立热门游戏候选清单，补充需要翻译且译名可靠的公共词库数据，并记录纳入/排除依据。
- [ ] 词库作为独立数据更新合入 `main`，不为每批词库数据创建新的插件版本。
- [ ] 复核固定同仓库 Raw 词库的请求、严格校验、缓存、离线回退和隐私文档；`v1.0.0` 不开放自定义远程 URL。
- [ ] 确认 PluginDatabase 对 Starlight `.star`、Lua 后端、根目录 `plugin.json` 和可变纯数据远程词库的当前要求。
- [x] 准备 PluginDatabase 英文预审草稿，只询问 Starlight 分发路径与固定远程纯数据政策；尚未对外发送。
- [ ] 若 PluginDatabase 不接受固定代码读取 `main` 可变词库，则切换为其认可的捆绑或不可变版本化词库方案。
- [x] 补充 `CHANGELOG.md`、网络行为/隐私、安全报告方式和支持范围说明。
- [x] 完成公共仓库审计与隔离克隆构建验证；补充 LF 策略并记录 PluginDatabase 当前构建脚本与 Starlight 模板的过渡期差异。
- [x] 将构建产物、敏感文件、本机路径、个人导入字段和本地远程地址基线纳入 `check:repository` 自动门禁。
- [ ] 完成官方模板要求的 Stable/Beta 自测、第三方 Stable/Beta 验证，以及两个其他插件 PR 的测试反馈链接。
- [ ] 发布并观察 `v1.0.0-rc.1`；只有出现阻断修复时才追加后续 RC。关闭所有阻断项后发布 `v1.0.0`，最后提交 PluginDatabase PR。

### `v0.2.0` 开发验收

- [x] 使用 AppID 直接查询预标准化的内存中文名索引，不读取磁盘、不发起网络请求或逐条 RPC。
- [x] 在 Steam 原生名称、排序名、AppID、建议和模糊匹配全部失败后追加中文译名匹配。
- [x] 名称显示和搜索使用两个独立声明式补丁；搜索前端不可用时保持 Steam 原生结果。
- [x] 两份独立搜索 fixture 覆盖唯一命中、混淆变量变化、重复应用和结构变化失配。
- [x] 当前本机 Steam 稳定版 `chunk~2dcc5aaf7.js` 中两个生产补丁均唯一命中。
- [x] 51 项 Vitest（含 5,000 条规模性能门禁）、4 组 Lua 测试和 184 条正式词库校验通过。
- [x] Windows 11 真机验证中文译名、Steam 英文原名和 AppID 查询均可命中。
- [x] Windows 11 真机验证连续输入、退格删除和清空搜索后的结果恢复。
- [x] 以用户约 1,000 款真实游戏库作为 Windows 11 真机功能基线。
- [x] 本地生成合法的 5,000 条临时词库和 5,000 个候选游戏；模拟数据不进入正式词库或发布包。
- [x] 性能用例累计运行 7 次：索引构建为 3.58–4.69 ms，针对 5,000 个候选执行 7 次连续中文查询为 4.28–6.21 ms，均低于各 250 ms 门禁。
- [x] 验证搜索补丁故意失配时英文搜索和中文名称显示互不影响。
- [x] 本地正式 `.star`、ZIP、SHA-256 与归档内容校验通过。
- [x] 从注入正式远程词库地址的候选 ZIP 完成空缓存安装复验。
- [x] 提交代码、创建 `v0.2.0` 标签，并复验 GitHub CI/Release 正式产物。

## 真机验证记录

### 2026-08-26：公共仓库与隔离构建审计

- 复核全部被跟踪文件，未发现密钥、账户数据、Steam ID、本机绝对路径、缓存、日志或构建产物；个人库导入记录保持脱敏。
- 从提交 `ceeb12e` 创建隔离克隆，以冻结锁文件从零安装依赖；51 项 Vitest、4 组 Lua 测试、692 条词库校验、正式 `.star`、四区段校验和 ZIP 内容校验全部通过。
- 发现 Windows 自动换行会在 `starlight lsp` 后制造三个无内容差异的伪修改；新增 `.gitattributes` 固定文本文件为 LF。
- 官方 PluginTemplate 已采用 Starlight 和 `millennium.toml`，但 PluginDatabase 当前分发脚本仍硬性要求旧式 `plugin.json`；该兼容边界须在提交前取得维护者确认，不以未经验证的兼容文件规避。
- 进一步锁定 PluginDatabase `97403bd` 和 PluginTemplate `af0a07c`：把数据库当前构建脚本原样用于本项目隔离构建，稳定复现缺少 `plugin.json` 的退出码 1；确认是官方模板与数据库流程尚未同步的外部门禁。
- PluginDatabase 当前规则要求全面审计网络访问但未明文禁止远程纯数据；本项目固定 GitHub Raw、零上传、禁重定向、TLS 校验、超时与严格数据校验具备送审基础，可变 `main` 词库是否绕过固定提交审核仍需明确确认。

### 2026-08-26：个人 Steam 库 70–79% 好评候选导入

- 用户确认上一轮 574 条个人词库测试构建已在 Steam 实机增加成功。
- 从简体中文客户端导出的差集中筛入全部 118 款好评率为 70–79% 的游戏；独立导入清单逐条保存 AppID、导出原名和中文译名。
- 当前词库由 574 条增至 692 条；该用户库中已无好评率达到 70% 的英文未覆盖游戏。
- 剩余脱敏清单更新为 115 款：低于 70% 共 110 款、暂无评价 5 款。
- TypeScript、Lua 静态检查、51 项 Vitest、4 组 Lua 后端测试、692 条词库校验、正式 `.star`、四区段校验和 ZIP 内容校验全部通过。
- 测试 `.star` SHA-256 为 `9528A18A5EB95148B60CCC5516FB43DAE72E7609D9D5E2E410AC3881E7DF547E`；安装文件哈希一致且再次通过四区段校验。
- 已确认插件缓存目录不存在；574 条旧版备份为 `steam-game-name-zh.pre-692-review-20260826-1133.star.bak`，随后覆盖安装 692 条版本。

### 2026-08-26：574 条个人词库测试构建安装

- 本地测试构建保持插件元数据版本 `0.2.0`，不创建发布标签；构建默认不配置远程地址，以内置 574 条词库作为本轮人工核对基线。
- TypeScript、Lua 静态检查、51 项 Vitest、4 组 Lua 后端测试、574 条词库校验、正式 `.star` 构建与四区段校验均通过。
- 测试 `.star` SHA-256 为 `13381F980D0E423B6A4C7DEF7EC731B5CA5214B5F460C8DD3F893EFD6922675C`；覆盖安装后文件哈希一致。
- 原正式插件已备份为 `steam-game-name-zh.pre-library-review-20260826-020048.star.bak`。
- 检查发现插件缓存仍为旧的 184 条远程词库；已将整个缓存目录移动为 `steam-game-name-zh.pre-574-review-20260826-020301`，避免旧缓存覆盖本轮内置数据。
- 用户重启 Steam 后确认新增词条已成功显示；继续以 70% 好评率为下一批导入门槛。

### 2026-08-26：个人 Steam 库首批词库筛选

- 输入为用户 2026-08-25 导出的完整 Steam 库，共 1,290 项；仅保留 `appType = 1` 的 1,083 款游戏，不处理工具、运行时和专用服务器。
- 其中 277 款当前显示名已经含汉字，183 款由原有词库覆盖；去重后得到 625 个英文候选。
- 首批按高置信常用简体中文译名筛入 99 款，导入清单只保留 AppID、英文原名与中文名，不保留账户 ID、库路径或拥有者数据。
- 导入工具逐条核对 AppID、游戏类型、原名、汉字状态和既有词库重复项；当前词库共 283 条，其中覆盖该用户库 282 款。
- TypeScript、Lua 静态检查、51 项 Vitest、4 组 Lua 后端测试及 283 条词库校验全部通过；剩余 526 个英文候选留待后续人工分批审核。

### 2026-08-26：个人 Steam 库高评价候选导入

- 将剩余 51 款 Steam 好评率不低于 90% 的候选全部纳入第二批个人词库；公开测试客户端与序章保留类型后缀，不与正式本体混淆。
- 有稳定简体中文标题的项目采用常用译名；仍以英文品牌名发布的项目采用保守音译或描述性个人译名，并在独立导入清单中保留 AppID 与原名对应关系。
- 当前内置词库由 283 条增至 334 条；该用户库的剩余英文候选降至 475 款。
- 生成脱敏的剩余候选 Markdown 清单：80–89% 共 242 款、70–79% 共 118 款、低于 70% 共 110 款、暂无评价 5 款。
- TypeScript、Lua 静态检查、51 项 Vitest、4 组 Lua 后端测试、334 条词库校验、正式 `.star` 构建、四区段校验和 ZIP 内容校验全部通过。

### 2026-08-26：个人 Steam 库 80–89% 好评候选导入

- 从简体中文客户端导出的差集中筛入全部 242 款好评率为 80–89% 的游戏；导入工具要求 `appType = 1`、当前显示名不含汉字且 AppID 尚未入库。
- 清理 2 条当前 Steam 名称已带中文的旧映射：`Grand Theft Auto V 传承版` 和 `全面战争：三国`，确保此类游戏保留 Steam 原名。
- 当前词库由 334 条净增至 574 条；该用户库中已无好评率达到 80% 的英文未覆盖游戏，也不存在“当前名称含汉字但仍在词库”的冗余映射。
- 剩余清单更新为 233 款：70–79% 共 118 款、低于 70% 共 110 款、暂无评价 5 款。
- 移除 GTA V 的正式词库条目后，同步清理同步启动回退表中的旧映射；51 项 Vitest、4 组 Lua 后端测试、574 条词库校验、正式 `.star`、四区段校验和 ZIP 内容校验全部通过。

### 2026-08-25：`v0.2.0` 正式发布与 GitHub Release 安装复验

- `main` 发布提交与 `v0.2.0` 标签均对应 `65dc459`；GitHub CI 与 Release 工作流成功完成。
- 从 GitHub Release 下载正式 ZIP 与 SHA-256 文件并核对一致；ZIP SHA-256 为 `7A28124A122B150250513BCA0711A89A6BD305F3A434BD87FC58D44D5D85BD67`。
- ZIP 仅包含规范命名的 `steam-game-name-zh.star`；正式 `.star` SHA-256 为 `958EE0F1CFA50402343BA1591C3C20F7807C251B15C534D2A333C44470CD4FEC`，安装文件哈希一致并通过 Starlight 四区段校验。
- 正式产物启动后完成远程词库检查，保留 184 条有效词库、正式 Raw 地址与 ETag；用户确认名称显示、中文/英文/AppID 搜索和词库状态均正确，`v0.2.0` 验收完成。

### 2026-08-25：`v0.2.0` 中文、英文与 AppID 搜索验证

- 将本地正式构建的 `v0.2.0` `.star` 直接覆盖安装到 Millennium 插件目录；安装文件与构建源文件 SHA-256 一致，并通过 Starlight 四区段校验。
- 用户启动 Steam 后确认使用中文译名“传送门”、Steam 英文原名“Portal”和 AppID“620”均能搜索到对应游戏。
- 结果确认中文搜索已融入游戏库左栏原生过滤流程，同时保留英文原名和 AppID 搜索能力。
- 用户确认连续输入搜索文本、逐字删除和完全清空后，左栏结果均能正确显示与恢复。
- 用户当前真实游戏库约 1,000 款；本地使用不写入正式数据的合法模拟词库与候选游戏，将自动化性能上限覆盖到 5,000 条。
- 7 次独立运行中，5,000 条索引构建耗时 3.58–4.69 ms，7 次连续中文查询总耗时 4.28–6.21 ms，显著低于各 250 ms 门禁。

### 2026-08-25：`v0.2.0` 搜索补丁独立失配降级验证

- 从隔离源码构建专用测试包，仅将搜索补丁目标文件改为必定不匹配；名称补丁、前后端代码和词库均与正常 `v0.2.0` 保持一致。
- 专用 `.star` 通过 Starlight 四区段校验，安装后 SHA-256 与构建源文件一致。
- 用户真机确认左栏中文/双语名称继续正常显示，英文原名“Portal”仍可搜索，而中文译名“传送门”不再命中。
- 结果确认搜索补丁能够独立安全降级，不影响名称显示补丁或 Steam 原生英文搜索。

### 2026-08-25：`v0.2.0` 候选 ZIP 空缓存安装验证

- 在隔离候选源码中注入正式远程地址 `https://raw.githubusercontent.com/mangataw/SteamGameName/main/data/translations.zh-CN.json`，其余生产代码与已验收版本一致。
- 候选 `.star` 通过 Starlight 四区段校验，SHA-256 为 `414D3E664EA11392A5C43C3DCA1CE544E2F1A676BE2B9A58BA6B835D61E5C25A`。
- 候选 ZIP 仅包含规范命名的 `steam-game-name-zh.star`，SHA-256 为 `28722B32DD50AF8630D7067C5DCC8EE5EF634D11735AEEB43704B58140B28978`。
- 安装前将原缓存移动到 `steam-game-name-zh.pre-v0.2.0-clean-install-20260825` 作为可恢复备份，确认正式缓存目录不存在后从候选 ZIP 安装。
- 空缓存启动于 2026-08-25 17:07:43 生成新的 `catalog.json` 和 `metadata.json`，取得 184 条 Schema 1 远程词库；元数据保存正式 Raw 地址、ETag 和成功更新时间。
- 用户确认中文/双语名称、中文译名、Steam 英文原名和 AppID 搜索均正常，设置页词库状态正确。

### 2026-08-25：`v0.1.8` 稳定版发布与安装复验

- `main` 提交与 `v0.1.8` 标签对应提交 `4848819`；GitHub CI 和 Release 工作流均成功。
- 从 GitHub Release 下载正式 ZIP 与 SHA-256 文件，校验一致后安装其中的 `steam-game-name-zh.star`，临时库导出版已停用并保留为 `.bak`。
- 用户重启 Steam 后确认插件结果正常：设置页显示 184 条词库，临时导出入口已移除，新增中文/双语名称正常显示。
- 用户确认游戏库滚动、右键菜单、详情页及启动游戏等基础交互正常，`v0.1.8` 稳定核心版验收完成。

### 2026-08-24：远程词库首次更新、304 与热更新验证

- 本地测试构建注入固定地址 `https://raw.githubusercontent.com/mangataw/SteamGameName/main/data/translations.zh-CN.json`，未开放用户自定义 URL。
- 首次启动自动取得 76 条远程词库并写入 `catalog.json` 与 `metadata.json`；状态为“最新”，来源为 `remote`。
- 手动刷新在远程内容未变化时保持 ETag、条目数和最后成功更新时间不变，仅更新上次检查时间，符合 304 行为。
- 真机发现 Lua `on_frontend_loaded` 在当前 Millennium 启动顺序下未可靠触发；自动检查已改由前端初始化完成后以 `force=false` 主动调用，重启后无需手动操作即可更新上次检查时间并进入“最新”状态。
- 缓存内容经 304 确认后仍以缓存作为物理数据来源；设置页将状态显示为“最新（已联网确认）”，来源保持简短的“缓存”，避免误认为没有联网检查并防止窄设置页换行。
- 向远程词库加入 AppID `779340`“全面战争：三国”和 AppID `761620`“旁观者 2”后，不重启 Steam 手动刷新，条目数从 76 增至 78，ETag 与成功更新时间同步更新。
- 用户真机确认 `Beholder 2` 在当前双语模式下立即显示为 `旁观者 2 | Beholder 2`。
- GitHub Raw 主分支更新存在短暂 CDN 传播延迟；等待主 URL 返回新 ETag 后刷新成功，不影响旧缓存继续使用。

### 2026-08-24：远程超时与缓存回退验证

- 使用只改变编译时远程地址的专用测试包，将请求指向不可响应端点；内置词库、缓存代码和前端逻辑均保持正式版本一致。
- 手动刷新约 8 秒后结束，设置页显示“离线”和“词库请求失败”；来源继续为 `cache`，78 条、ETag、最后成功更新时间及 Beholder 2 双语名称均保持不变。
- 完整重启 Steam 后仍从 `cache` 加载 78 条词库，Beholder 2 名称正常，确认缓存可以跨会话保留。
- 测试期间发现 Millennium 将旧 `%STEAM%\ext` 搬入 `millennium-migration-temp`；恢复缓存后再次重启未重复迁移，判定为一次性 Millennium 目录迁移，而非插件持续性缓存路径故障。

### 2026-08-24：补丁失配安全降级验证

- 使用仅将补丁目标文件模式改为必定不匹配值的专用测试包，其他后端、前端、词库和远程更新逻辑均与正常构建一致。
- Steam 主窗口和游戏库正常启动，Beholder 2 恢复 Steam 英文原名，未出现空白名称、启动循环或 SteamUI 崩溃。
- 设置页正常完成联网检查并显示 78 条缓存词库，同时出现“当前 Steam 客户端左栏结构暂不兼容，插件已安全停用名称补丁”的兼容性提示。
- 正常补丁首次渲染会通过 FFI 上报兼容状态；正常构建真机未显示误报警告。
- 304 状态文案收敛为标题“最新（已联网确认）”与短来源“缓存”，在当前窄设置页中保持单行显示。

### 2026-08-24：禁用与卸载验证

- 用户真机确认禁用插件后 Steam 恢复原始名称，功能正常。
- 用户真机确认卸载插件后 Steam 恢复原始名称，功能正常。

### 2026-08-24：`v0.1.7` 显示模式与配置界面修复确认

- 用户真机确认双语/仅中文选项可以切换并生效。
- 用户真机确认完整重启 Steam 后仍保持所选显示模式。
- 用户真机确认词库状态与最近错误不再显示乱码。
- 用户真机确认显示模式改为下一行宽布局、短选项后显示正常。
- 用户真机确认左栏搜索框行为正常。
- 用户真机确认左栏右键菜单正常，并可正常启动游戏。
- 用户真机确认虚拟列表滚动正常，滚动后加载的条目保持当前显示模式。
- 用户真机确认切换显示模式后右侧游戏详情页保持正常。
- 用户真机确认切换显示模式后下载页不受影响。

### 2026-08-24：`v0.1.7` 显示模式修复自动化验证

- 配置界面只保留“双语”和“仅中文”，不再使用在 Steam 设置页中选择无效的原生 HTML `select`。
- 后端只接受两种模式，并将历史 `original` 配置持久迁移为默认 `bilingual`。
- 设置持久化成功后，前端存储会原子发布新快照；失败时保持现状并显示错误。
- 前端兼容 RPC 返回 JSON 字符串或已解码对象，避免重复 `JSON.parse` 导致设置、词库和状态快照全部加载失败。
- RPC JSON 使用 ASCII-only Unicode 转义跨越 FFI，状态和错误使用稳定代码并由前端本地化，避免中文乱码。
- 显示模式控件改为独占下一行的宽布局，选项缩短为“双语”和“仅中文”，完整格式放在说明文字中。
- 左栏补丁从一次性字符串格式化改为挂载订阅前端存储的响应式组件，同时保留启动回退与长度门禁。
- 结果：34 项 Vitest、4 组 Lua 测试、Lua 静态检查、76 条词库校验、正式 `.star` 构建、ZIP 和 SHA-256 打包检查全部通过。
- `v0.1.7` 自动化与真机门禁已全部通过。

### 2026-08-24：Windows 11 中文界面与 70 个游戏验证

- 环境：Windows 11、Steam 稳定版简体中文界面、本地离线安装。
- 结果：70 个游戏在左侧列表正常显示，滚动未观察到卡顿；Steam 离线模式下名称正常显示。
- 范围：右侧详情保持 Steam 原始内容，符合“只修改左栏”的核心约束。
- 已知问题：设置页面存在三种模式选项，但切换后左栏名称不能即时变化。
- 范围决策：核心版收敛为双语和仅中文两种模式；中文搜索不在 `v0.1.x` 修补，独立列入 `v0.2.0`。

### 2026-08-24：`v0.1.4` 扩充词库后 SteamUI 无法启动

- 环境：Windows，Steam 桌面客户端，Millennium `v3.4.1`。
- 现象：Steam 更新/验证窗口关闭后主窗口不出现，独立推广窗口也随 Steam Web UI 一起消失；`steam.exe` 和 Millennium 后端存活，但没有 `steamwebhelper.exe`。
- 隔离：禁用插件后 Steam 正常启动，重新启用 `v0.1.4` 后问题复现。
- 定位：完整 70 条词库被复制进 Hooking API 替换字符串；自动化 fixture 能验证 JavaScript 语法，但不能覆盖 Millennium/SteamUI 对长替换的运行时兼容性。
- `v0.1.5` 处理：完整词库移至前端初始快照与后端资源，补丁只保留不超过 8 条、编码后小于 1 KiB 的固定启动回退；真机启用后 SteamUI 正常启动，新增中文名正常显示。

### 2026-08-24：`v0.1.0` 发布包未出现在插件列表

- 环境：Windows，Steam 桌面客户端，Millennium `v3.4.1`。
- 现象：将发布 ZIP 解压到本机实际插件目录后，Millennium 插件列表仍为空。
- 定位：ZIP 内只有无扩展名的 `steam-game-name-zh`；该文件本身可通过 Starlight v2 四区段校验，但 Millennium 只识别 `.star` 插件包。
- 临时规避：将文件重命名为 `steam-game-name-zh.star` 后重新启动 Steam。
- 下版本验收：正式构建直接生成 `.star` 文件；ZIP 保留该扩展名；自动化检查归档条目名并运行 `starlight verify`；在干净的 Millennium `v3.4.1` 环境解压、重启后，插件列表能够显示“Steam 游戏名中文化”。

## 后续发布路径

1. [x] 完成 `v0.1.7` 两种显示模式收敛和即时切换修复。
2. [x] 发布并验收 `v0.1.8-rc.2`。
3. [x] 发布并验收 `v0.1.8` 稳定核心版。
4. [x] 独立实现 `v0.2.0` 中文搜索代码、内存索引、生产补丁和自动化测试。
5. [x] 完成 `v0.2.0` Windows 11 真机、性能、安全降级和候选发布包验收。
6. [x] 提交并发布 `v0.2.0`，复验 GitHub CI/Release 正式产物。
7. [x] 将启动期配置菜单偏移/消失确认为 Millennium 上游问题，记录跨 Extendium 复现和临时规避方案。
8. [ ] 扩充面向公共发布的热门游戏词库，完成数据来源与质量审核；作为数据更新合入，不创建插件版本。
9. [ ] 完成网络行为、隐私、安全报告、变更记录和干净构建等公共仓库审计。
10. [ ] 锁定固定远程词库政策并取得 PluginDatabase 对可变纯数据来源的确认；不开放自定义 URL。
11. [ ] 完成 PluginDatabase 元数据与构建形式预审，并在 RC 文档中披露上游菜单问题。
12. [ ] 发布并观察 `v1.0.0-rc.1`，完成第三方 Stable/Beta 验证与社区测试要求；仅在阻断修复后追加 RC。
13. [ ] 发布 `v1.0.0`，复验正式产物后向 `SteamClientHomebrew/PluginDatabase` 提交 PR。
14. [ ] 在不占用 RC 主线的独立阶段向 Millennium 提交菜单竞态报告并持续跟踪；只有需要插件兼容调整时才插入 `v0.2.1`。
