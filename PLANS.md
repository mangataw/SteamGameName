# Steam 游戏名称中文化插件完整开发计划

## 1. 项目概述

### 1.1 项目名称

- 项目工作名：SteamGameName
- 插件显示名：Steam 游戏名中文化
- 插件内部名：`steam-game-name-zh`
- 技术平台：SteamBrew / Millennium

### 1.2 项目目标

开发一个集成在 Windows Steam 桌面客户端中的 Millennium 插件，对“游戏库左侧列表”内的游戏名称进行中文化显示。

插件以 Steam AppID 作为唯一匹配依据，从同一 GitHub 仓库获取人工维护的简体中文词库。Steam 原始名称已经包含中文时不做任何修改；没有中文且词库中存在对应 AppID 时，按照用户选择显示中文名、原名或双语名称。

### 1.3 首版成功标准

首个稳定版本必须满足以下条件：

1. 仅修改 Windows Steam 桌面客户端游戏库左侧列表，不影响其他页面。
2. 以 Steam AppID 精确匹配翻译，不使用模糊名称匹配。
3. 默认显示格式为 `中文名 | 原名`。
4. 支持“中文名 | 原名”“仅中文名”两种模式，切换后无需重启 Steam 即时生效。
5. 原名称中已经包含汉字时保持 Steam 原始显示。
6. GitHub 不可访问时使用最后一次有效缓存，不阻塞 Steam 启动。
7. 禁用插件后恢复 Steam 原始名称，不残留永久修改。
8. Steam UI 结构变化导致补丁失效时安全降级，不影响 Steam 正常运行。

## 2. 范围定义

### 2.1 首版范围

- Windows 11。
- Steam 桌面模式。
- Steam 客户端简体中文界面。
- 游戏库左侧列表中的官方 Steam 游戏条目。
- 同仓库 GitHub Raw 词库更新。
- 本地词库缓存与离线回退。
- Millennium 插件设置页面。
- 自动化构建、测试、词库校验与发布打包。

### 2.2 首版不包含

- Big Picture、Steam Deck 游戏模式或移动端。
- Linux 和 macOS。
- 游戏详情页、库首页卡片、下载页、通知、商店/全局搜索结果和弹窗。
- `v0.1.x` 不包含按中文译名过滤游戏库左栏；该能力独立安排在 `v0.2.0`。
- 非 Steam 快捷方式。
- 工具、软件、原声等非游戏类型的主动翻译。
- 在线机器翻译或运行时调用第三方翻译服务。
- 自动抓取 Steam 商店中文名称。
- 用户自定义 AppID 映射或自定义远程词库地址。
- 修改 Steam 本地数据库、清单文件或服务器数据。

### 2.3 后续候选范围

首版稳定后再单独评估：

- `v0.2.0` 增加游戏库左栏中文搜索，同时保留英文原名搜索。
- Linux 支持。
- Windows 10 和 Steam 英文界面兼容性。
- Big Picture 和 Steam Deck 界面。
- 游戏详情页、下载页等更多显示位置。
- 繁体中文词库。
- 本地用户覆盖词库。
- 词库分片、增量更新与镜像源。

## 3. 用户体验与业务规则

### 3.1 显示模式

定义以下固定枚举：

```ts
type DisplayMode = "bilingual" | "chinese";
```

| 模式 | 命中词库时的显示 | 默认 |
| --- | --- | --- |
| `bilingual` | `中文名 | 原名` | 是 |
| `chinese` | `中文名` | 否 |

双语模式分隔符固定为半角空格、竖线、半角空格，即 ` | `。首版不允许用户自定义分隔符。

首版不提供“仅原名”显示模式。需要完全恢复 Steam 原名时禁用插件；如果后续确认禁用插件的操作成本过高，再单独评估语义明确的“启用名称中文化”开关。

### 3.2 名称转换规则

名称格式化函数接收 `appId` 和 Steam 当前提供的 `originalName`，按以下顺序执行：

1. AppID 不是有效正整数时返回原名。
2. 原名为空或不是字符串时不执行替换。
3. 原名包含汉字时直接返回原名，不追加词库名称。
4. 词库没有对应 AppID 时返回原名。
5. 模式为 `chinese` 时返回中文词库名称。
6. 模式为 `bilingual` 时返回 `中文名 | 原名`。

“包含汉字”的判定至少覆盖常用汉字与扩展汉字 Unicode 范围。标点、日文假名或韩文字符不能单独视为中文名称。

### 3.3 设置页面

Millennium 插件设置内容包括：

- 显示模式下拉选择。
- 当前数据来源与远程 ETag 的短标识（如可用）。
- 最后成功更新时间；优先显示 HTTP `Last-Modified`，缺失时显示本地成功下载时间。
- 有效翻译条目数量。
- 上次检查时间。
- 当前数据状态：内置、缓存、最新、离线或错误。
- “立即刷新词库”按钮。
- 最近一次刷新错误的简短说明。

设置变更和成功刷新必须立即触发左侧列表重新渲染，不要求重启 Steam。

## 4. 技术基线

### 4.1 技术栈

- 插件框架：Millennium 当前稳定插件 API。
- 构建工具：`@steambrew/starlight`。
- 前端：TypeScript、React、Millennium 前端 API。
- 后端：LuaJIT、Millennium Lua API。
- 数据格式：UTF-8 JSON。
- 包管理和脚本运行：Bun。
- CI/CD：GitHub Actions。

### 4.2 插件组成

- TypeScript/React 前端负责设置界面、名称格式化状态和左栏名称渲染。
- Lua 后端负责网络请求、缓存、配置持久化、数据校验和前后端 RPC。
- Starlight 补丁负责将翻译组件限制性注入游戏库左侧列表。
- GitHub 仓库同时存储插件代码、词库和发布工作流。

## 5. 建议目录结构

```text
SteamGameName/
├─ backend/
│  ├─ main.lua
│  ├─ rpc_functions.lua
│  ├─ catalog.lua
│  ├─ cache.lua
│  ├─ settings.lua
│  ├─ validation.lua
│  └─ patches.lua
├─ frontend/
│  ├─ index.tsx
│  ├─ components/
│  │  ├─ SettingsContent.tsx
│  │  └─ TranslatedGameName.tsx
│  ├─ services/
│  │  ├─ backend.ts
│  │  └─ translationStore.ts
│  ├─ translation/
│  │  ├─ formatDisplayName.ts
│  │  └─ containsHan.ts
│  └─ types/
│     ├─ catalog.ts
│     └─ settings.ts
├─ data/
│  ├─ translations.zh-CN.json
│  └─ translations.schema.json
├─ scripts/
│  ├─ validate-catalog.ts
│  ├─ generate-source-config.ts
│  └─ package-release.ts
├─ tests/
│  ├─ frontend/
│  ├─ backend/
│  ├─ fixtures/
│  └─ patch/
├─ .github/
│  ├─ workflows/
│  │  ├─ ci.yml
│  │  └─ release.yml
│  ├─ PULL_REQUEST_TEMPLATE.md
│  └─ ISSUE_TEMPLATE/
├─ plugin.json
├─ package.json
├─ bun.lock
├─ tsconfig.json
├─ README.md
├─ CONTRIBUTING.md
├─ LICENSE
└─ PLANS.md
```

实际实现可以合并很小的模块，但必须维持数据加载、显示规则和 UI 补丁之间的边界，避免补丁代码直接处理网络或文件系统。

## 6. 数据协议

### 6.1 词库文件

主数据文件固定为：

```text
data/translations.zh-CN.json
```

结构如下：

```json
{
  "schemaVersion": 1,
  "games": {
    "620": "传送门 2"
  }
}
```

### 6.2 字段约束

- `schemaVersion`：正整数；首版只接受 `1`。
- `games`：以十进制 AppID 字符串为键、简体中文名为值的对象。
- AppID 必须是无前导符号的正整数，并且不得重复。
- 中文名去除首尾空白后不能为空，必须包含至少一个汉字。
- 中文名不得包含控制字符、HTML 或脚本内容。
- 文件必须为 UTF-8，禁止 BOM。
- 序列化时按 AppID 数值升序排列，减少无意义的 Git 差异。

### 6.3 TypeScript 类型

```ts
interface TranslationCatalog {
  schemaVersion: 1;
  games: Record<string, string>;
}

interface PluginSettings {
  schemaVersion: 1;
  displayMode: DisplayMode;
}

type CatalogSource = "bundled" | "cache" | "remote";

interface CatalogStatus {
  source: CatalogSource;
  entryCount: number;
  etag: string | null;
  lastModified: string | null;
  lastCheckedAt: string | null;
  lastSuccessfulUpdateAt: string | null;
  error: string | null;
}
```

`revision` 和 `updatedAt` 不放入人工维护的词库。远程内容是否变化由 GitHub ETag 判断；更新时间来自 HTTP `Last-Modified`，若响应未提供该字段，则由插件记录本地成功下载时间。贡献者只需维护 AppID 映射。

### 6.4 远程地址生成

发布构建通过 GitHub Actions 的 `${GITHUB_REPOSITORY}` 生成固定数据地址：

```text
https://raw.githubusercontent.com/<owner>/<repo>/main/data/translations.zh-CN.json
```

本地构建未获得仓库标识时不猜测用户名，使用打包的内置词库，并在开发日志中说明远程刷新未配置。

首版不接受用户输入的 URL，不跟随跨主机重定向，也不执行词库中的任何内容。

## 7. 运行时架构与数据流

### 7.1 启动流程

1. Lua 后端加载最小模块并在一秒内调用 `millennium.ready()`。
2. 后端读取设置文件；不存在或损坏时使用默认设置。
3. 后端加载内置词库。
4. 若本地存在有效缓存，则将其作为最后一次已知有效的远程词库并覆盖内置词库；缓存无效时继续使用内置词库。
5. 前端加载当前词库和设置快照，注册翻译状态存储。
6. Starlight 补丁仅在确认目标为库左侧列表名称组件后挂载翻译组件。
7. Steam 前端加载完成后，后端执行一次非阻塞远程检查。
8. 远程数据有效且发生变化时写入缓存，并通知前端刷新状态。

### 7.2 名称渲染流程

```text
Steam 左栏条目
  → 取得 AppID 与 Steam 原名
  → TranslatedGameName 订阅翻译状态
  → 应用汉字检测、词库查询和显示模式
  → 输出纯文本名称
```

翻译组件只返回文本内容，不注入 HTML，不改变点击事件、可访问性属性、React key、列表排序或 Steam 数据模型。

### 7.3 状态更新

- 前端维护只读目录映射、显示模式和目录状态。
- 设置变更先持久化成功，再更新前端状态。
- 刷新成功后使用一次原子状态替换并发出版本事件。
- 每个已挂载的名称组件仅在目录版本或显示模式变化时重新计算。
- 禁止通过遍历整个 DOM 周期性重写文本。

## 8. Lua 后端设计

### 8.1 RPC 接口

通过 Starlight FFI 暴露以下函数：

```text
get_catalog() -> JSON string
get_catalog_status() -> JSON string
refresh_catalog(force: boolean) -> JSON string
get_settings() -> JSON string
set_display_mode(mode: string) -> JSON string
```

所有 RPC 返回统一结果：

```json
{
  "ok": true,
  "data": {},
  "error": null
}
```

失败时 `ok` 为 `false`，`data` 为 `null`，`error` 为适合用户理解但不包含本地敏感路径的错误码和简短信息。

### 8.2 远程刷新

- 请求超时建议为 8 秒。
- 使用固定 User-Agent，包含插件名和版本。
- 缓存 ETag，并通过 `If-None-Match` 请求头检查更新。
- 保存响应中的 `Last-Modified`；响应未提供时使用本地成功下载时间作为展示时间。
- `304` 仅更新最后检查时间。
- `200` 响应必须先完成大小、JSON、Schema 和业务字段校验。
- 最大响应体首版限制为 5 MiB。
- 至多进行两次请求；第二次采用短退避，避免阻塞和请求风暴。
- 网络、TLS、HTTP 或校验失败均保留当前有效目录。
- 同一 Steam 会话内自动刷新最多一次；手动刷新可以绕过该限制。

### 8.3 缓存与持久化

后端在插件自己的可写目录维护：

```text
cache/catalog.json
cache/metadata.json
config/settings.json
```

写入流程：写临时文件、刷新并关闭、重新读取验证、原子重命名替换正式文件。不得在验证前覆盖最后一次有效缓存。

缓存元数据保存远程 URL 标识、ETag、HTTP `Last-Modified`、最后成功下载时间和最后检查时间，不保存 Steam 账户信息或使用情况。这些字段由插件自动维护，不进入人工编辑的词库文件。

### 8.4 日志

- `info`：插件版本、加载的数据来源、条目数、ETag 短标识和刷新结果。
- `warn`：网络失败、使用旧缓存、补丁未命中。
- `error`：内置词库无效、缓存无法读写、RPC 参数不合法。
- 不记录 Steam 用户名、账户 ID、游戏库完整列表或目录内容。

## 9. 前端与 UI 补丁设计

### 9.1 补丁原则

- 只补丁左侧库列表中负责显示应用名称的最小渲染位置。
- 使用多个结构信号确认目标，不仅依赖易变化的混淆变量名或单个 CSS 类名。
- 补丁替换前验证预期匹配数；匹配数不是唯一值时停止应用。
- 补丁只将原始名称表达式包装为插件组件，不修改 Steam Store 或应用对象。
- 禁用或卸载时注销补丁、监听器和状态订阅。

### 9.2 补丁兼容性

为每个受支持的 Steam 稳定版 UI 样本保留精简 fixture。CI 对 fixture 执行补丁匹配测试：

- 精确命中目标位置。
- 不命中相似但不属于左栏的组件。
- 结构变化时明确失败，而不是生成不确定替换。

运行时补丁失败后，设置页面显示“当前 Steam 客户端版本暂不兼容”，并提供复制非敏感诊断信息的能力作为后续增强；首版至少必须记录日志。

### 9.3 性能要求

- 单次名称计算只进行一次 AppID 哈希查询和短字符串检测。
- 目录解析只在启动或刷新时执行一次。
- 不为每个列表条目发起网络请求或后端 RPC。
- 不使用 MutationObserver 扫描整个 Steam DOM。
- 对大型游戏库滚动保持 Steam 原有虚拟列表行为。

## 10. 安全与隐私

- 只访问编译时确定的同仓库 HTTPS Raw URL。
- 保持 TLS 证书验证开启。
- 对响应大小、状态码、Schema、字符和 AppID 进行严格校验。
- 词库仅作为字符串映射，不允许执行代码、加载 HTML 或动态模块。
- 插件不上传用户游戏库、Steam ID、账户信息或遥测。
- 插件不读取 Steam 登录凭据、Cookie 或令牌。
- 日志不得包含完整本地路径中的用户名部分。
- 依赖使用 lockfile 固定，发布构建应可从 Git 标签复现。

## 11. 自动化测试计划

### 11.1 名称规则单元测试

至少覆盖：

- 英文原名、命中词库、双语模式。
- 英文原名、命中词库、仅中文模式。
- 英文原名、未命中词库。
- 已有简体中文原名。
- 已有繁体中文原名。
- 中英文混合原名。
- 只有日文假名、只有韩文或只有标点的原名。
- 无效、缺失或超出范围的 AppID。
- 空名称、前后空格、emoji、引号、竖线和 Unicode 组合字符。
- 中文名与原名相同的异常词库数据。

### 11.2 词库校验测试

- 正常 Schema 1 文件。
- 不支持的 Schema 版本。
- 非数字、负数、零和带前导符号的 AppID。
- 空译名、无汉字译名、控制字符和 HTML 标签。
- 重复 AppID。
- 缺失或非法的 `schemaVersion`、缺失 `games`。
- 损坏的 ETag、`Last-Modified` 或本地下载时间缓存元数据。
- 超过大小限制的数据。
- 排序检查和标准化输出。

### 11.3 后端测试

- 无缓存首次启动。
- 有效缓存启动。
- 缓存损坏后退回内置目录。
- 远程 `200` 更新。
- 远程 `304` 未修改。
- DNS、TLS、超时和非 2xx 错误。
- 远程 JSON 有效但 Schema 错误。
- 临时文件写入或重命名失败。
- 设置损坏和非法模式输入。
- 并发点击刷新时只保留一个刷新任务。

### 11.4 补丁测试

- 对受支持的 Steam 左栏 fixture 精确匹配一次。
- 对详情页和搜索 fixture 匹配零次。
- 混淆变量变化但结构信号保持时仍能匹配。
- 关键结构改变时安全失败。
- 重复加载插件不会应用两次补丁。
- 卸载后所有注册项被清理。

## 12. 手工验收计划

### 12.1 环境矩阵

- Windows 11 x64。
- Steam 当前稳定版。
- Millennium 当前稳定版。
- 小型库与大型库。
- Steam 客户端简体中文界面。
- 在线、离线、GitHub 被阻断或请求超时环境。

### 12.2 核心验收场景

1. 安装并启用插件后，左侧列表命中项显示 `中文名 | 原名`。
2. Steam 原名已经包含中文的项目保持不变。
3. 未命中词库的项目保持原名。
4. 设置切换为仅中文后立即更新当前可见和随后滚动出现的条目。
5. 设置切回双语后立即更新当前可见和随后滚动出现的条目。
6. 重启 Steam 后设置仍然保留。
7. 更新 GitHub 词库并点击刷新后，无需重启即可看到新名称。
8. 断网重启时使用缓存，不出现空白名称或持续错误弹窗。
9. 禁用插件后恢复原始名称。
10. 英文原名搜索、分类折叠、拖动滚动条、右键菜单和启动游戏行为正常。
11. 详情页、下载页和其他页面没有被修改。
12. 补丁模拟失配时 Steam 正常加载，插件只记录兼容性错误。

## 13. CI/CD 与发布

### 13.1 Pull Request CI

每个 PR 必须执行：

1. 安装锁定依赖。
2. TypeScript 类型检查。
3. 前端单元测试。
4. Lua 静态检查或语法检查。
5. 词库 Schema 与业务规则校验。
6. 补丁 fixture 测试。
7. Starlight 正式构建。
8. 发布包内容检查。

任何词库校验、补丁匹配或构建失败都必须阻止合并。

### 13.2 发布工作流

- 版本采用语义化版本，例如 `v0.1.0`。
- Git 标签触发正式构建。
- 构建时注入仓库 Raw 数据地址和插件版本。
- 生成包含运行必需文件的 ZIP，不包含测试、源码映射、缓存和本地配置。
- 生成校验和并附加到 GitHub Release。
- Release Notes 说明支持的 Steam/Millennium 版本、内置词库条目数、对应源码提交和已知限制；这些信息由发布流程自动生成。

### 13.3 官方插件库

完成稳定测试后，按照 Millennium PluginDatabase 要求提交插件仓库。后续插件代码更新通过新版本发布；词库更新可以直接合并到主分支，由运行时刷新获取，不强制发布插件新版本。

## 14. 词库维护流程

### 14.1 新增或修正条目

贡献者提交 PR，内容至少包含：

- Steam AppID。
- 当前 Steam 原名。
- 建议的简体中文名。
- 中文名来源或选择理由。
- 若为修正，说明原翻译的问题。

### 14.2 审核标准

- AppID 与目标游戏一致。
- 优先采用 Steam 官方简体中文名称。
- 没有官方名称时采用广泛使用、无歧义的社区译名。
- 避免广告性、戏谑性、机器直译或包含版本营销文字的名称。
- 同一系列名称风格保持一致。
- 不为 Steam 当前原名已经包含中文的条目添加无意义映射。

### 14.3 合并后的处理

- 校验脚本自动排序和检查。
- 不需要人工更新版本号或时间字段；GitHub 自动生成新的内容 ETag，插件在下次检查时识别变化。
- 合并至 `main` 后，Raw 文件成为新的远程词库。
- 插件用户在下一次启动检查或手动刷新时获得更新。

## 15. 开发阶段与里程碑

### 阶段一：项目骨架与规则核心

- 基于官方 PluginTemplate 初始化仓库。
- 配置 Bun、TypeScript、Starlight 和测试框架。
- 定义词库 Schema、设置类型和名称转换函数。
- 完成词库校验脚本与初始测试数据。

交付标准：项目可以构建，名称规则和数据校验测试全部通过。

### 阶段二：后端数据服务

- 实现内置目录加载、缓存和设置持久化。
- 实现 GitHub ETag 更新与错误回退。
- 实现 RPC 接口和结构化日志。
- 完成后端失败场景测试。

交付标准：在线更新、离线缓存、损坏回退和设置持久化均可独立验证。

### 阶段三：左栏 UI 集成

- 定位当前 Steam 左栏名称渲染点。
- 实现限制性 Starlight 补丁。
- 实现翻译状态存储和响应式名称组件。
- 添加 fixture 与补丁失配测试。

交付标准：仅左侧列表名称发生变化，禁用后完全恢复。

### 阶段四：设置与体验完善

- 实现显示模式选择、状态信息和手动刷新。
- 添加刷新中的禁用状态和错误提示。
- 优化大型库滚动性能和重复渲染。
- 完成 Windows 手工测试矩阵。

交付标准：双语与仅中文两种模式即时生效，断网和大型库场景稳定。

### 阶段五：发布与维护准备

- 完成 CI、Release 打包和校验和。
- 编写安装、卸载、贡献和故障排查文档。
- 发布预览版并收集不同 Steam 客户端环境的兼容性结果。
- 修复核心阻断问题后发布 `v0.1.8` 稳定核心版。
- `v0.2.0` 中文搜索稳定后再评估 `v1.0.0` 与 PluginDatabase 提交。

交付标准：可复现发布包通过全部自动化和手工验收。

### 15.1 `v0.1.7`：显示模式修复

- 将设置枚举、后端校验、格式化逻辑和设置 UI 收敛为 `bilingual`、`chinese` 两种模式。
- 修复模式切换后 Steam 左栏不重新渲染的问题；不得依赖重启 Steam。
- 验证当前可见条目和虚拟列表滚动后出现的条目使用同一模式。
- 验证设置持久化，重启 Steam 后保持用户选择。
- 补充前端状态更新、后端设置迁移和补丁回归测试；历史 `original` 配置应安全迁移到默认 `bilingual`。
- 不在本版本加入中文搜索补丁。

交付门禁：两种模式即时切换和持久化均通过自动化与 Windows 11 真机验证，左栏之外的页面不受影响。

### 15.2 `v0.1.8-rc.2` / `v0.1.8`：核心版最终验收

- 在 Steam 稳定版、Millennium 目标版本和简体中文界面完成 Release ZIP 干净安装。
- 验证在线、离线、GitHub 不可达和 8 秒请求超时场景；失败时继续使用有效缓存或内置词库。
- 验证远程词库更新后无需重启即可更新名称。
- 验证禁用、重新启用和卸载后名称完整恢复，无残留补丁或监听器。
- 验证补丁失配时 Steam 正常启动并安全降级。
- 使用至少 70 个游戏复测滚动、分类折叠、英文原名搜索、右键菜单和启动游戏。
- RC 阶段只接受阻断性修复，不再加入中文搜索等新功能。

交付门禁：上述项目全部有真机记录，CI、正式构建、`.star`、ZIP 与 SHA-256 校验全部通过后发布 `v0.1.8`。

### 15.3 `v0.2.0`：中文搜索

- 仅扩展游戏库左栏搜索框，不修改商店搜索、全局搜索或其他页面。
- 中文译名和 Steam 英文原名始终都可命中，搜索行为不依赖当前显示模式。
- 搜索只读取内存词库，不修改 Steam 应用对象、数据库或清单文件。
- 名称显示补丁与搜索补丁保持独立；搜索补丁失配时仅退回英文原名搜索，中文名称显示继续工作。
- 为支持的 Steam 稳定版保存独立搜索 fixture，覆盖唯一匹配、中文/英文命中、未命中、重复应用和结构变化安全失败。
- `v0.2.0` 以约 1,000 款真实游戏库作为真机功能基线，并在本地测试进程中生成合法的 5,000 条词库与 5,000 个候选游戏，验证索引构建和连续查询性能；模拟数据不得写入正式词库或发布包。
- 本地性能门禁暂定为：5,000 条索引构建小于 250 ms，针对 5,000 个候选游戏连续执行 7 次中文查询的总耗时小于 250 ms。
- 始终不使用全页面 MutationObserver 或逐条后端 RPC。

交付门禁：中文和英文查询结果正确，搜索补丁可独立降级，且不扩大左栏之外的修改范围。

## 16. 风险与缓解措施

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| Steam 更新导致左栏组件结构变化 | 翻译失效 | 多信号补丁、fixture 测试、安全降级、快速兼容版本 |
| 补丁误命中其他组件 | 修改超出范围 | 要求唯一匹配并验证上下文，匹配异常时不应用 |
| GitHub Raw 在部分网络环境不可用 | 无法获取新词库 | 内置目录、持久缓存、短超时、手动刷新、无阻塞启动 |
| 远程数据损坏或被错误提交 | 显示异常 | Schema、业务校验、大小限制、原子缓存替换、PR CI |
| 大型游戏库性能下降 | 滚动卡顿 | 内存映射、纯函数格式化、状态批量更新、禁止 DOM 全量扫描 |
| 名称翻译存在争议 | 社区维护成本增加 | 明确来源优先级、PR 讨论和可追溯修订历史 |
| Millennium API 变化 | 构建或运行失效 | 锁定依赖、标注支持版本、升级前执行完整兼容测试 |

## 17. 完成定义

只有满足以下全部条件，首版才视为完成：

- 计划范围内功能全部实现。
- 自动化测试和正式构建全部通过。
- Windows 手工验收矩阵无阻断问题。
- GitHub 词库更新和离线缓存经过真实环境验证。
- 插件只修改游戏库左侧列表。
- 补丁失配能够安全降级。
- 安装、设置、刷新、禁用和卸载文档完整。
- 发布 ZIP 可在干净 Millennium 环境安装运行。
- 仓库不包含密钥、用户数据、缓存或机器特定路径。

## 18. 已锁定的默认决策

- 首版平台：Windows Steam 桌面版。
- 首版界面：仅游戏库左侧列表。
- 匹配主键：官方 Steam AppID。
- 默认显示：`中文名 | 原名`。
- 可选显示：仅中文名；首版不提供仅原名模式。
- 中文搜索：与显示模式解耦，独立安排在 `v0.2.0`。
- 已含汉字的 Steam 原名：保持不变。
- 数据维护：人工维护 AppID 词库，不使用机器翻译。
- 数据位置：与插件代码相同的 GitHub 仓库。
- 更新方式：启动检查和手动刷新，失败使用有效缓存。
- 网络安全：固定同仓库 HTTPS Raw 地址，不允许用户自定义。
- Windows 10、Steam 英文界面、非 Steam 项目、工具、软件、Big Picture 和 Linux：不在当前首版范围内。

## 19. 参考基线

- Millennium 官方插件文档：<https://docs.steambrew.app/plugins/>
- 官方 PluginTemplate：<https://github.com/SteamClientHomebrew/PluginTemplate>
- Millennium 项目：<https://github.com/SteamClientHomebrew/Millennium>
- 官方插件数据库：<https://github.com/SteamClientHomebrew/PluginDatabase>
